# Copy to .env.local for local overrides, or set in Vercel → Project → Settings → Environment Variables.
# Required for production builds (Vite bakes this in at build time).
VITE_API_BASE_URL=https://your-railway-service.up.railway.app
